using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ObjectTypes
{
    NULL = -1,
    IDLE = 1,
    JUMP = 2,
    ATTACK = 3,
    RUN = 4,
    DEAD = 5,
    VICTORY = 6,
}
